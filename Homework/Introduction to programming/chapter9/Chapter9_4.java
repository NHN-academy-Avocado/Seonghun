import java.io.*;

public class Chapter9_4 {

  public static void main(String[] args) {
    // int n=10;
    // int[] v = new int[n];
    // FileReader f = new FileReader("dati.txt");
    // BufferedReader in = new BufferedReader(f);
    // int i=0;
    // String linea = in.readLine();
    // while (linea!=null) {
    //   v[i] = Integer.parseInt(linea);
    //   linea = in.readLine();
    //   i++;
    // }
    // f.close();

    // file이 존재하지 않아서 오류가 생긴다.
    // IOException을 throw 해주지 않아서 오류가 생긴다.
    // Unhandled Exception type FileNotFoundException [Ln8, 열 20]
    // Unhandled exception type IOException [Ln 11, 열 20]
    // Unhandled exception type IOException [Ln 14, 열 15]
    // Unhandled exception type IOException [Ln 17, 열 5]
  }
}